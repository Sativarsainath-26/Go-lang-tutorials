package main

import (
	"fmt"
)

func main() {
	a := 42
	b := 42
	fmt.Println(a == b) // output is true which is a boolean value. we can see here the expression is evaluated to a booelan value

	// In the go lang there is no === triple equal.
	// In the go lang single equal is an = assignment operator
	// In the go lang double equal is an == conditional operator.

}
